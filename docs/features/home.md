# Home

**Status:** stub — placeholder only.

- Page: `src/pages/Home.tsx`
- Route: `/home` (protected, no role requirement beyond being signed in)

Nothing else is wired. Whoever builds this should replace this file's content with what the page
actually does once implemented — see `docs/wip/` first in case someone else has already claimed it.
